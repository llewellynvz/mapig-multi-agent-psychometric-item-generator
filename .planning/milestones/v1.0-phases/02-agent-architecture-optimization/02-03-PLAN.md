---
phase: 02-agent-architecture-optimization
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - app/prompts/bias_reviewer.md
  - tests/test_prompts.py
  - tests/test_bias_reviewer.py
autonomous: true
requirements: [AGT-07, AGT-08]

must_haves:
  truths:
    - Bias Reviewer prompt defines all 7 bias types with clear descriptions
    - Structured 7-part checklist enables comprehensive single-pass evaluation
    - Intersectional bias detection explicitly prompted in checklist
    - Severity escalation rule for intersectional flags (≥4) specified
  artifacts:
    - path: app/prompts/bias_reviewer.md
      provides: Enhanced Bias Reviewer prompt with 7-type taxonomy and structured checklist
      min_lines: 100
      contains: ["7 bias types", "structured checklist", "intersectional bias", "severity escalation"]
  key_links:
    - from: app/agents/bias_reviewer.py
      to: app/prompts/bias_reviewer.md
      via: load_prompt() function
      pattern: "load_prompt.*bias_reviewer"
---

<objective>
Optimize Bias Reviewer prompt with 7-type bias taxonomy and structured checklist for comprehensive single-pass evaluation with intersectional bias detection.

Purpose: Transform Bias Reviewer into research-backed bias detection engine that catches intersectional effects without multi-pass overhead.

Output: Enhanced bias_reviewer.md prompt with 7 bias types, structured checklist format, and intersectional synthesis (Russell & Kaplan 2021).
</objective>

<execution_context>
@C:/Users/Llewellyn/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Llewellyn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-architecture-optimization/02-CONTEXT.md
@.planning/phases/02-agent-architecture-optimization/02-RESEARCH.md

Key research findings (from 02-RESEARCH.md):
- 7 bias types: construct, linguistic, cultural reference, socioeconomic, context access, protected attribute, intersectional
- Single comprehensive pass recommended (vs multi-pass) for cost/latency optimization
- Structured checklist format with explicit intersectional bias section
- Intersectional bias auto-escalates to severity ≥4 (research shows 4-8x more sensitive)
- Chain-of-thought reasoning required per user decision

Current pattern: Bias Reviewer returns ReviewComment objects with severity 1-5.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance Bias Reviewer prompt with 7-type taxonomy and structured checklist</name>
  <files>app/prompts/bias_reviewer.md, tests/test_prompts.py, tests/test_bias_reviewer.py</files>
  <behavior>
    - Test: test_bias_reviewer_7_types verifies 7 bias types defined in prompt
    - Test: test_structured_checklist verifies structured checklist format present
  </behavior>
  <action>
RED: Unskip tests in tests/test_prompts.py (test_bias_reviewer_7_types) and tests/test_bias_reviewer.py (test_structured_checklist). Run tests (should fail).

GREEN: Update app/prompts/bias_reviewer.md:

**Keep existing structure** (Role, Inputs, Output format sections).

**Add "7 Bias Types Taxonomy" section:**
```
## 7 Bias Types Taxonomy

Evaluate items for bias across these dimensions:

1. Construct bias
   - Item assumes culture-bound meaning of construct
   - Relies on norms varying across groups
   - Example: "I value independence" (collectivist vs individualist cultures)

2. Linguistic bias
   - Idioms, phrases, or words with differential familiarity
   - Complex vocabulary disadvantaging non-native speakers
   - Example: "I hit the ground running" (idiom)

3. Cultural reference bias
   - Assumes knowledge of culture-specific practices, values, contexts
   - Example: "I celebrate major holidays with family" (assumes holiday observance)

4. Socioeconomic bias
   - Assumes resources, opportunities, or experiences not universally shared
   - Example: "I have a private workspace at home" (housing assumptions)

5. Context access bias
   - Assumes specific work arrangement, role level, shift work
   - Example: "I collaborate with colleagues in person" (remote workers disadvantaged)

6. Protected attribute bias
   - References or stereotypes related to gender, race, ethnicity, religion, citizenship, disability
   - Example: "As a working mother, I balance career and family" (gender + parental status)

7. Intersectional bias
   - Combined identity effects where multiple protected attributes interact
   - Compounding disadvantage for specific identity combinations
   - Example: Item with both socioeconomic + context access bias compounds for low-income shift workers
```

**Add "Structured Checklist Evaluation Process" section:**
```
## Evaluation Process

For each item, use this structured checklist:

### Step 1: Evaluate each bias type

1. Construct bias: [✓/✗] [brief note if flagged]
2. Linguistic bias: [✓/✗] [brief note if flagged]
3. Cultural reference bias: [✓/✗] [brief note if flagged]
4. Socioeconomic bias: [✓/✗] [brief note if flagged]
5. Context access bias: [✓/✗] [brief note if flagged]
6. Protected attribute bias: [✓/✗] [brief note if flagged]

### Step 2: Intersectional bias check

If ≥2 types flagged above, examine combined identity effects:
- Do multiple bias types compound for certain identities?
- Example: Office work assumption (context access) + white-collar role (socioeconomic) = compounds for blue-collar, remote, shift workers

7. Intersectional bias: [✓/✗] [brief note if flagged with specific identity combinations]

### Step 3: Generate ReviewComment (only if ≥1 type flagged)

- severity: Use highest individual type severity (1-5 scale)
- **Severity escalation rule**: If intersectional bias flagged, severity automatically ≥4 (major issue)
- issue: Describe flagged bias type(s) and intersectional effect if any
- suggested_edit: Rewrite item addressing all flagged types
- reasoning: Explain which types detected and why (chain-of-thought)

### Step 4: Return results

- Items with no flags: Skip (no ReviewComment)
- Items with ≥1 flag: Return ReviewComment with all flagged types noted
```

**Add "Chain-of-Thought Reasoning" section:**
```
## Reasoning Requirements

Each ReviewComment.reasoning must explain:
1. Which bias type(s) detected and specific language triggering flag
2. Which identity groups disadvantaged and how
3. If intersectional: how multiple types compound effect
4. Why suggested_edit addresses flagged bias

Keep reasoning technical and explicit (3-5 sentences).
```

Run tests (should pass). Commit.

REFACTOR: Review prompt for clarity. Ensure checklist format is clear and actionable. Run tests (should still pass). Commit if changes made.
  </action>
  <verify>
    <automated>pytest tests/test_prompts.py -k "bias_reviewer" tests/test_bias_reviewer.py -x</automated>
  </verify>
  <done>bias_reviewer.md contains 7 bias types with definitions, structured checklist format, intersectional bias check, severity escalation rule, and chain-of-thought requirements. All tests pass.</done>
</task>

</tasks>

<verification>
Final verification commands:
```bash
# Verify all Bias Reviewer tests pass
pytest tests/test_prompts.py -k "bias_reviewer" tests/test_bias_reviewer.py -v

# Verify prompt file updated
wc -l app/prompts/bias_reviewer.md  # Should be ~100+ lines

# Verify all 7 bias types present
grep -i "construct bias\|linguistic bias\|cultural reference bias\|socioeconomic bias\|context access bias\|protected attribute bias\|intersectional bias" app/prompts/bias_reviewer.md | wc -l  # Should be ≥7

# Verify structured checklist present
grep -i "structured checklist\|✓/✗\|Step 1\|Step 2" app/prompts/bias_reviewer.md | wc -l  # Should be ≥5
```
</verification>

<success_criteria>
- bias_reviewer.md enhanced with 7-type bias taxonomy (definitions + examples)
- Structured checklist format with 4-step evaluation process
- Intersectional bias check explicitly prompted after evaluating 7 types
- Severity escalation rule specified (intersectional flags ≥4)
- Chain-of-thought reasoning requirements added
- All 2 Bias Reviewer tests pass (AGT-07, AGT-08)
- Single comprehensive pass approach (not multi-pass) per research recommendation
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-architecture-optimization/02-03-SUMMARY.md`
</output>
