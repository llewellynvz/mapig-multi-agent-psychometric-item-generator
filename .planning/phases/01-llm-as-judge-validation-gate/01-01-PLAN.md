---
phase: 01-llm-as-judge-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_validator.py
  - tests/test_graph.py
  - tests/test_llm_factory.py
  - tests/test_schemas.py
autonomous: true
requirements: [VAL-02, VAL-03, VAL-04, VAL-05, VAL-07]

must_haves:
  truths:
    - Test scaffold exists for validation agent unit tests
    - Test scaffold exists for graph integration tests
    - Test scaffold exists for LLM factory Claude model selection
    - Test scaffold exists for validation schema validation
  artifacts:
    - path: "tests/test_validator.py"
      provides: "Unit tests for validation agent"
      min_lines: 50
    - path: "tests/test_graph.py"
      provides: "Integration tests for validation gate routing"
      min_lines: 40
    - path: "tests/test_llm_factory.py"
      provides: "Tests for Claude model factory"
      min_lines: 30
    - path: "tests/test_schemas.py"
      provides: "Tests for validation schemas"
      min_lines: 30
  key_links:
    - from: "tests/test_validator.py"
      to: "app.agents.validator"
      via: "import"
      pattern: "from app\\.agents\\.validator import"
    - from: "tests/test_graph.py"
      to: "app.graph"
      via: "import"
      pattern: "from app\\.graph import"
---

<objective>
Create test scaffolding for all validation functionality to enable TDD workflow for subsequent plans.

Purpose: Establish test-first development for validation features, ensuring each component has automated verification
Output: Pytest test files with test scaffolds (functions with `pytest.skip()` or `assert False`) for all 9 validation requirements
</objective>

<execution_context>
@C:/Users/Llewellyn/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Llewellyn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm-as-judge-validation-gate/01-RESEARCH.md

## Existing Test Structure

```python
# From tests/test_smoke.py - existing pattern
import pytest
from app.schemas import UserRequest

def test_smoke():
    req = UserRequest(
        construct_name="Belonging",
        construct_definition="Sense of connection",
        target_population="Adults",
        response_scale="5-point Likert",
        item_count=5
    )
    assert req.construct_name == "Belonging"
```

## Test Requirements from Research

Per RESEARCH.md Section "Validation Architecture > Phase Requirements → Test Map":
- VAL-01: `test_validation_placement` - validation executes after item writer
- VAL-02: `test_four_dimensions` - validates 4 scoring dimensions
- VAL-03: `test_cot_reasoning` - validates chain-of-thought reasoning
- VAL-04: `test_score_range` - validates 1-10 scale
- VAL-05: `test_rejection_threshold` - validates ≥7.0 threshold
- VAL-06: `test_retry_limit` - validates max 3 attempts
- VAL-07: `test_validator_uses_opus` - validates Claude Opus model selection
- VAL-08: `test_validation_in_response` - validates API response includes scores
- VAL-09: `test_validation_export` - validates export metadata

Pytest is already installed (9.0.2 per RESEARCH.md).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation agent test scaffold</name>
  <files>tests/test_validator.py</files>
  <action>
Create pytest test file for validation agent with test scaffolds for:
- `test_four_dimensions`: Assert ValidationResponse contains 4 DimensionScore objects (correspondence, distinctiveness, clarity, specificity) - VAL-02
- `test_cot_reasoning`: Assert each DimensionScore has non-empty reasoning field before score - VAL-03
- `test_score_range`: Assert all scores are integers between 1-10 inclusive - VAL-04
- `test_rejection_threshold`: Assert items with weighted_score < 7.0 have accept=False - VAL-05

Each test should:
1. Use `pytest.skip("Awaiting validator agent implementation")` or `assert False, "TODO: implement"`
2. Include docstring explaining what behavior it verifies
3. Import from `app.agents.validator` (not yet created - will fail until Plan 02)
4. Use realistic test data (sample UserRequest, DraftItem list)

Follow existing test_smoke.py pattern for imports and structure.
  </action>
  <verify>
    <automated>pytest tests/test_validator.py -v --collect-only</automated>
  </verify>
  <done>File exists with 4 test functions, all properly decorated with pytest.skip or marked as TODO, pytest collection succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create graph integration and LLM factory test scaffolds</name>
  <files>tests/test_graph.py, tests/test_llm_factory.py, tests/test_schemas.py</files>
  <action>
Create three test scaffold files:

**tests/test_graph.py:**
- `test_validation_placement`: Assert validation_node executes between item_writer_node and reviewers_fanout_node in graph structure - VAL-01
- `test_retry_limit`: Assert max 3 regeneration attempts before accepting best score - VAL-06

**tests/test_llm_factory.py:**
- `test_validator_uses_opus`: Assert get_validator_model() returns ChatAnthropic with model="claude-opus-4-6" - VAL-07
- `test_claude_api_key_required`: Assert get_validator_model() raises ValueError if CLAUDE_API_KEY not set

**tests/test_schemas.py:**
- `test_validation_export`: Assert FinalOutput schema can serialize validation_results with all dimension scores, reasoning, and attempt count - VAL-09
- `test_dimension_score_schema`: Assert DimensionScore validates score range 1-10

Each test should use `pytest.skip()` with descriptive message. Include realistic fixtures where needed (sample GraphState, Settings override).
  </action>
  <verify>
    <automated>pytest tests/test_graph.py tests/test_llm_factory.py tests/test_schemas.py -v --collect-only</automated>
  </verify>
  <done>Three test files exist with 6 total test functions, all skipped with clear messages, pytest collection succeeds</done>
</task>

</tasks>

<verification>
Run full test discovery to ensure all scaffolds are properly structured:
```bash
pytest tests/ -v --collect-only
```

Should show:
- tests/test_validator.py: 4 tests collected
- tests/test_graph.py: 2 tests collected
- tests/test_llm_factory.py: 2 tests collected
- tests/test_schemas.py: 2 tests collected
- tests/test_smoke.py: 1 test collected (existing)
Total: 11 tests collected
</verification>

<success_criteria>
- All test scaffold files exist and are syntactically valid Python
- Pytest collects all 10 new test functions (4+2+2+2) without errors
- Each test has clear docstring and skip message linking to requirement ID
- Test structure follows existing test_smoke.py patterns
- No tests execute (all skipped) - implementation comes in later waves
</success_criteria>

<output>
After completion, create `.planning/phases/01-llm-as-judge-validation-gate/01-01-SUMMARY.md`
</output>
