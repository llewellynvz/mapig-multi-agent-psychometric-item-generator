---
phase: 05-vercel-deployment
plan: 03
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md
  - .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md
autonomous: false
requirements:
  - DEP-05
  - DEP-06
  - DEP-07
  - DEP-08

user_setup:
  - service: vercel
    why: "Serverless deployment platform"
    env_vars:
      - name: CLAUDE_API_KEY
        source: "Anthropic Console -> API Keys"
      - name: OPENAI_API_KEY
        source: "OpenAI Platform -> API Keys (optional)"
      - name: APP_MODE
        source: "Set to 'claude' or 'openai'"
      - name: NEXT_PUBLIC_API_URL
        source: "Backend Vercel Function URL (after backend deployment)"
    dashboard_config:
      - task: "Create backend project"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository"
      - task: "Configure backend environment variables"
        location: "Backend Project -> Settings -> Environment Variables"
      - task: "Create frontend project"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository (set Root Directory to 'frontend')"
      - task: "Configure frontend environment variable"
        location: "Frontend Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - Production URL is accessible publicly
    - Backend /healthz endpoint returns healthy status
    - Frontend loads and displays setup form
    - SSE streaming works in production
    - Environment variables are configured in Vercel
    - DEP-08 deferral documented with baseline measurements
  artifacts:
    - path: .planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md
      provides: Step-by-step deployment walkthrough
      min_lines: 100
    - path: .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md
      provides: Cold start performance baseline and DEP-08 deferral documentation
      min_lines: 20
  key_links:
    - from: Frontend Vercel deployment
      to: Backend Vercel Function
      via: NEXT_PUBLIC_API_URL environment variable
      pattern: "https://.*\\.vercel\\.app"
---

<objective>
Deploy MAPIG to Vercel production with environment variable configuration, end-to-end verification, and cold start baseline measurement.

Purpose: Complete production deployment to Vercel infrastructure, verify all functionality works in serverless environment, and document DEP-08 (cold start optimization) deferral with baseline performance measurements.

Output: Working production URL with documented deployment steps, verification results, and cold start baseline for future optimization.
</objective>

<execution_context>
@C:/Users/Llewellyn/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Llewellyn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vercel-deployment/05-CONTEXT.md
@.planning/phases/05-vercel-deployment/05-RESEARCH.md
@.planning/phases/05-vercel-deployment/05-01-PLAN.md
@.planning/phases/05-vercel-deployment/05-02-PLAN.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive deployment guide</name>
  <files>.planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md</files>
  <action>
Create detailed deployment walkthrough in .planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md:

```markdown
# MAPIG Vercel Deployment Guide

**Date:** 2026-03-09
**Phase:** 05-vercel-deployment
**Status:** Ready for deployment

## Overview

This guide walks through deploying MAPIG to Vercel serverless infrastructure with:
- Backend: Python FastAPI serverless function
- Frontend: Next.js standalone application
- Checkpointing: In-memory (ephemeral, no cross-request persistence)
- SSE Streaming: Supported (300s timeout on Pro plan)

## Prerequisites

- [ ] Vercel account (free or Pro plan)
- [ ] Git repository pushed to GitHub/GitLab/Bitbucket
- [ ] Anthropic API key (for Claude models)
- [ ] OpenAI API key (optional, for OpenAI models)
- [ ] Perplexity API key (optional, for web search)

**Recommended:** Vercel Pro plan for 300s execution timeout (Hobby plan has 60s limit which may be insufficient)

## Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Vercel Platform                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Frontend (Next.js)              Backend (FastAPI)           │
│  ┌──────────────────┐            ┌──────────────────┐        │
│  │ UI Components    │────────────>│ api/index.py     │        │
│  │ - Setup Form     │ HTTPS/SSE  │ - ASGI app       │        │
│  │ - Progress UI    │<───────────│ - LangGraph      │        │
│  │ - Results Table  │            │ - Agents         │        │
│  └──────────────────┘            └──────────────────┘        │
│                                                               │
│  Environment:                    Environment:                │
│  NEXT_PUBLIC_API_URL             CLAUDE_API_KEY              │
│                                  OPENAI_API_KEY              │
│                                  APP_MODE                    │
└─────────────────────────────────────────────────────────────┘
```

## Step-by-Step Deployment

### Step 1: Deploy Backend

1. **Log in to Vercel Dashboard**: https://vercel.com/dashboard

2. **Create New Project**:
   - Click "Add New..." → "Project"
   - Select your Git provider (GitHub/GitLab/Bitbucket)
   - Import MAPIG repository

3. **Configure Backend Project**:
   - **Project Name**: `mapig-backend` (or your preference)
   - **Framework Preset**: Other
   - **Root Directory**: Leave as `.` (monorepo root)
   - **Build Command**: Leave empty or `echo 'No build needed'`
   - **Output Directory**: Leave empty
   - **Install Command**: `pip install -r requirements.txt` or `poetry install` (Vercel auto-detects)

4. **Configure Environment Variables** (Backend Project → Settings → Environment Variables):

   Required:
   - `CLAUDE_API_KEY`: Your Anthropic API key
   - `APP_MODE`: `claude` (or `openai` if using OpenAI)

   Optional:
   - `OPENAI_API_KEY`: Your OpenAI API key (if using OpenAI models)
   - `SEARCH_PROVIDER`: `perplexity` (for web search)
   - `PERPLEXITY_API_KEY`: Your Perplexity API key
   - `PERPLEXITY_DOMAIN_FILTER`: Comma-separated domains (e.g., `apa.org,jstor.org`)

   All environments: Production, Preview, Development

5. **Deploy Backend**:
   - Click "Deploy"
   - Wait for deployment to complete (typically 1-3 minutes)
   - Note the production URL (e.g., `https://mapig-backend.vercel.app`)

6. **Verify Backend Deployment**:
   ```bash
   curl https://mapig-backend.vercel.app/healthz
   ```
   Expected response: `{"status": "healthy"}`

### Step 2: Deploy Frontend

1. **Create New Project** (separate from backend):
   - Click "Add New..." → "Project"
   - Import same repository (or use monorepo detection)

2. **Configure Frontend Project**:
   - **Project Name**: `mapig-frontend` (or your preference)
   - **Framework Preset**: Next.js
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build` (auto-detected)
   - **Output Directory**: `.next` (auto-detected)
   - **Install Command**: `npm install` (auto-detected)

3. **Configure Environment Variables** (Frontend Project → Settings → Environment Variables):

   Required:
   - `NEXT_PUBLIC_API_URL`: Backend production URL from Step 1.6 (e.g., `https://mapig-backend.vercel.app`)

   All environments: Production, Preview, Development

4. **Deploy Frontend**:
   - Click "Deploy"
   - Wait for deployment to complete (typically 2-4 minutes)
   - Note the production URL (e.g., `https://mapig-frontend.vercel.app`)

5. **Verify Frontend Deployment**:
   - Visit frontend URL in browser
   - Setup form should load
   - No CORS errors in console

### Step 3: End-to-End Verification

1. **Test Health Check**:
   ```bash
   curl https://mapig-backend.vercel.app/healthz
   ```
   Expected: `{"status": "healthy"}`

2. **Test Frontend Load**:
   - Visit `https://mapig-frontend.vercel.app`
   - Verify setup form renders
   - Open browser DevTools Console
   - No CORS errors should appear

3. **Test Item Generation** (manual - requires valid API keys):
   - Fill out setup form:
     - Construct name: "Conscientiousness"
     - Definition: "The tendency to be organized, responsible, and hardworking"
     - Model provider: Claude (or OpenAI if configured)
     - Item count: 5
   - Click "Generate Items"
   - Verify:
     - Progress indicator shows agent steps
     - SSE events stream in real-time
     - Generation completes within 60s
     - Results table displays items
     - No timeout errors

4. **Verify SSE Streaming**:
   - During generation, open Network tab
   - Find `/v1/generate-items-stream` request
   - Verify EventStream content-type
   - Events should stream incrementally (not all at once)

5. **Test Export Functionality**:
   - After generation completes
   - Click "Download" button
   - Select format (CSV/JSON/Markdown)
   - Verify file downloads with correct content

## Verification Checklist

- [ ] Backend deployed to Vercel
- [ ] Frontend deployed to Vercel
- [ ] Environment variables configured (CLAUDE_API_KEY, NEXT_PUBLIC_API_URL)
- [ ] /healthz endpoint returns healthy
- [ ] Frontend loads without errors
- [ ] Setup form renders correctly
- [ ] Item generation completes successfully
- [ ] SSE streaming works (progress updates visible)
- [ ] Results display with validation scores
- [ ] Export downloads work (all formats)
- [ ] No CORS errors in browser console
- [ ] No timeout errors (<300s execution time)

## Troubleshooting

### Backend Issues

**Problem**: 500 error on /healthz
- **Cause**: Environment variables missing or app failed to initialize
- **Fix**: Check Vercel logs (Backend Project → Deployments → Latest → View Function Logs)
- **Verify**: Environment variables are set correctly (exact spelling)

**Problem**: "Module not found" errors in logs
- **Cause**: Dependencies not installed
- **Fix**: Verify pyproject.toml or requirements.txt is in repository root
- **Verify**: Vercel build logs show "Installing dependencies"

**Problem**: "Approved sources directory not found"
- **Cause**: data/approved_sources not in repository or excluded by .vercelignore
- **Fix**: Verify data/ directory is committed and not in .vercelignore

### Frontend Issues

**Problem**: CORS error when calling backend
- **Cause**: NEXT_PUBLIC_API_URL incorrect or CORS not configured
- **Fix**: Verify NEXT_PUBLIC_API_URL matches backend URL exactly (no trailing slash)
- **Verify**: Backend CORS middleware allows *.vercel.app pattern

**Problem**: "Failed to fetch" errors
- **Cause**: Backend URL incorrect or backend not deployed
- **Fix**: Check NEXT_PUBLIC_API_URL in Vercel dashboard
- **Verify**: Backend /healthz endpoint accessible

**Problem**: Blank page or build errors
- **Cause**: Frontend build failed
- **Fix**: Check Vercel build logs (Frontend Project → Deployments → Latest → View Build Logs)
- **Verify**: npm run build succeeds locally

### Generation Issues

**Problem**: Timeout after 60s
- **Cause**: Function timeout too low (Hobby plan default)
- **Fix**: Upgrade to Pro plan or reduce ITEM_COUNT
- **Verify**: Check Vercel plan limits (Settings → Usage)

**Problem**: "API key not configured" error
- **Cause**: CLAUDE_API_KEY or OPENAI_API_KEY missing in backend environment
- **Fix**: Add environment variable in Vercel dashboard, redeploy
- **Verify**: Variable appears in Settings → Environment Variables

**Problem**: Generation starts but doesn't complete
- **Cause**: SSE connection dropped or function timeout
- **Fix**: Check Vercel function logs for errors
- **Verify**: Request completes within timeout limit

## Known Limitations (v1)

1. **Session Resumption**: Not available after cold start
   - Checkpoints stored in-memory only
   - Browser refresh loses in-progress generation
   - Acceptable trade-off for v1 (avoids external database)

2. **Cold Start Latency**: 3-8 seconds on first request
   - Python serverless cold start
   - Subsequent requests are fast (warm function)
   - See COLD-START-BASELINE.md for measurements
   - Optimization deferred to v2 (DEP-08)

3. **Execution Timeout**: 300s maximum (Pro plan)
   - Most runs complete in 20-40s
   - Complex constructs may take longer
   - Can configure up to 800s with Fluid Compute if needed

## Post-Deployment

### Custom Domain (Optional)

1. Backend Project → Settings → Domains
2. Add custom domain (e.g., `api.mapig.com`)
3. Follow DNS configuration instructions
4. Update frontend NEXT_PUBLIC_API_URL to custom domain

### Monitoring

- Vercel Analytics: Automatically enabled (page views, performance)
- Function Logs: Available in Deployments → View Function Logs
- Error Tracking: Check logs for exceptions

### Next Steps

After successful deployment:
- [ ] Update STATE.md with production URL
- [ ] Share production URL with stakeholders
- [ ] Monitor Vercel usage and function execution times
- [ ] Consider Phase 6: Comprehensive Evaluation Framework

## Support

- Vercel Documentation: https://vercel.com/docs
- MAPIG Issues: https://github.com/your-repo/issues
- Vercel Support: support@vercel.com (Pro plan only)

---

*Deployment guide created: 2026-03-09*
*Phase: 05-vercel-deployment*
```

Rationale: Comprehensive guide reduces deployment friction and documents verification steps, troubleshooting, and known limitations per user decisions in 05-CONTEXT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md && grep -q "Step-by-Step Deployment" .planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md</automated>
  </verify>
  <done>DEPLOYMENT-GUIDE.md created with prerequisites, architecture, step-by-step deployment, verification checklist, troubleshooting, and known limitations</done>
</task>

<task type="auto">
  <name>Task 2: Document DEP-08 deferral and measure cold start baseline</name>
  <files>.planning/phases/05-vercel-deployment/COLD-START-BASELINE.md</files>
  <action>
Create COLD-START-BASELINE.md documenting DEP-08 deferral decision and baseline measurements:

```markdown
# Cold Start Performance Baseline

**Date:** 2026-03-09
**Phase:** 05-vercel-deployment
**Requirement:** DEP-08 (Cold start optimization <5s)
**Status:** DEFERRED TO v2

## Decision Summary

Per 05-CONTEXT.md user decision: Accept longer cold starts for v1 deployment.

**Rationale:**
- Typical Python serverless cold starts: 3-8 seconds
- Warm function performance is more critical (majority of user requests hit warm functions)
- Optimization techniques (lazy imports, dependency pruning, warming strategies) add complexity
- Focus v1 on core functionality and deployment stability
- DEP-08 optimization deferred to v2 phase

## Baseline Measurements

**Methodology:**
Cold start measurements taken in Vercel production environment after initial deployment.

**Measurement approach:**
1. Trigger function cold start (wait 5+ minutes for function idle)
2. Make request to /healthz endpoint
3. Measure time from request initiation to first byte received
4. Repeat 5 times for statistical accuracy
5. Record Vercel function logs for internal initialization time

**Expected baseline (pre-deployment):**
- Cold start: 3-8 seconds (typical Python 3.12 serverless)
- Warm request: <500ms

**Actual baseline (to be measured post-deployment):**

| Attempt | Cold Start Time | Notes |
|---------|----------------|-------|
| 1       | TBD            | Initial cold start after deployment |
| 2       | TBD            | After 5 min idle |
| 3       | TBD            | After 10 min idle |
| 4       | TBD            | After 15 min idle |
| 5       | TBD            | After 20 min idle |

**Average cold start:** TBD seconds

**Warm request baseline:**

| Request | Response Time | Notes |
|---------|--------------|-------|
| 1       | TBD          | Immediate after cold start |
| 2       | TBD          | Sequential request |
| 3       | TBD          | Sequential request |

**Average warm response:** TBD milliseconds

## v2 Optimization Strategies

When DEP-08 becomes priority in v2, consider these approaches:

1. **Lazy imports:** Defer heavy imports (LangGraph, LLM clients) until first use
2. **Dependency pruning:** Remove unused dependencies to reduce bundle size
3. **Code splitting:** Move rarely-used features to separate functions
4. **Warming strategy:** Periodic ping to keep function warm during business hours
5. **Cold start monitoring:** Track cold start frequency and user impact
6. **Vercel Fluid Compute:** Leverage platform optimizations for faster cold starts

## Acceptance Criteria for v2

When revisiting DEP-08 in v2:
- [ ] Cold start consistently <5s (95th percentile)
- [ ] Optimization doesn't degrade warm function performance
- [ ] Monitoring shows reduced user-visible cold start impact
- [ ] Cost/benefit analysis justifies optimization complexity

## References

- User decision: .planning/phases/05-vercel-deployment/05-CONTEXT.md (lines 39-44)
- Research findings: .planning/phases/05-vercel-deployment/05-RESEARCH.md (Cold start section)
- Vercel documentation: https://vercel.com/docs/functions/limitations (Performance section)

---

*Created: 2026-03-09*
*Requirement: DEP-08*
*Status: Deferred to v2 per user decision*
```

Rationale: Documents the deferral decision with clear rationale, establishes baseline measurement methodology, and provides roadmap for v2 optimization. This satisfies the checker requirement for acknowledging DEP-08 even though it's deferred.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md && grep -q "DEP-08" .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md && grep -q "DEFERRED TO v2" .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md</automated>
  </verify>
  <done>COLD-START-BASELINE.md created documenting DEP-08 deferral, baseline measurement methodology, and v2 optimization roadmap</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Deploy MAPIG to Vercel</name>
  <action>Deploy MAPIG to Vercel using DEPLOYMENT-GUIDE.md</action>
  <instructions>
**What to do:**

Follow the step-by-step deployment guide in `.planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md` to deploy MAPIG to Vercel.

**Required actions:**

1. **Backend Deployment**:
   - Create Vercel project for backend
   - Configure environment variables (CLAUDE_API_KEY, APP_MODE)
   - Deploy and verify /healthz endpoint

2. **Frontend Deployment**:
   - Create Vercel project for frontend (Root Directory: `frontend`)
   - Configure NEXT_PUBLIC_API_URL with backend URL
   - Deploy and verify frontend loads

3. **End-to-End Verification**:
   - Test health check endpoint
   - Test item generation workflow
   - Verify SSE streaming works
   - Test export functionality

**Expected outcomes:**
- Backend URL accessible: `https://your-backend.vercel.app/healthz` returns `{"status": "healthy"}`
- Frontend URL accessible: `https://your-frontend.vercel.app` loads setup form
- Item generation completes successfully
- No CORS errors in browser console

**Verification checklist:**
- [ ] Backend deployed to Vercel
- [ ] Frontend deployed to Vercel
- [ ] Environment variables configured
- [ ] /healthz returns healthy
- [ ] Frontend loads without errors
- [ ] Item generation works end-to-end
- [ ] SSE streaming displays progress
- [ ] Export functionality works

**Time estimate:** 30-45 minutes (first deployment)

**Common issues:**
- CORS errors: Verify NEXT_PUBLIC_API_URL matches backend URL exactly
- Environment variables: Must redeploy after adding new variables
- Timeout errors: Verify Vercel Pro plan for 300s timeout

**After completion:**
Reply with:
- Backend production URL
- Frontend production URL
- Verification status (all checks passed or issues encountered)
  </instructions>
  <resume-signal>Provide backend URL, frontend URL, and verification status</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 2: Measure cold start baseline performance</name>
  <action>Measure cold start performance and update COLD-START-BASELINE.md</action>
  <instructions>
**What to do:**

Measure actual cold start performance of deployed Vercel function and record in COLD-START-BASELINE.md.

**Required actions:**

1. **Trigger cold starts** (5 measurements):
   - Wait 5+ minutes between attempts for function to go idle
   - Use browser Network tab or `curl` with timing
   - Measure time from request start to first byte

2. **Measurement commands**:
   ```bash
   # Using curl with timing
   curl -w "@-" -o /dev/null -s "https://your-backend.vercel.app/healthz" <<'EOF'
   time_namelookup: %{time_namelookup}\n
   time_connect: %{time_connect}\n
   time_starttransfer: %{time_starttransfer}\n
   time_total: %{time_total}\n
   EOF
   ```

   Or use browser DevTools:
   - Open Network tab
   - Clear cache and hard reload after idle period
   - Note "Waiting (TTFB)" time for /healthz request

3. **Record measurements** in COLD-START-BASELINE.md:
   - Update "Actual baseline" table with measured times
   - Calculate average cold start time
   - Also measure 3 warm requests (sequential) for comparison

4. **Update the file**:
   - Replace "TBD" entries with actual measurements
   - Add any observations (e.g., "First cold start slower due to initialization")

**Expected results:**
- Cold start: 3-8 seconds (acceptable for v1)
- Warm requests: <500ms

**After completion:**
Reply with:
- Average cold start time
- Average warm request time
- Confirmation that COLD-START-BASELINE.md has been updated
  </instructions>
  <resume-signal>Provide average cold start time, warm request time, and file update confirmation</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Verify production deployment</name>
  <what-built>
Production deployment of MAPIG on Vercel with:
- Backend FastAPI serverless function with in-memory checkpointing
- Frontend Next.js application with environment-based API URL
- Environment variables configured (CLAUDE_API_KEY, NEXT_PUBLIC_API_URL)
- CORS configured for Vercel domains
- Comprehensive deployment documentation
- Cold start baseline measurements documented
  </what-built>
  <how-to-verify>
**1. Backend Health Check**
```bash
curl https://[your-backend-url]/healthz
```
Expected: `{"status": "healthy"}`

**2. Frontend Accessibility**
- Visit `https://[your-frontend-url]` in browser
- Verify setup form loads
- Open DevTools Console (F12)
- Verify no CORS errors

**3. End-to-End Generation Test**
- Fill setup form:
  - Construct: "Extraversion"
  - Definition: "The tendency to be outgoing, sociable, and energetic"
  - Model: Claude (or OpenAI if configured)
  - Item count: 5
- Click "Generate Items"
- Verify:
  - Progress indicator shows agent steps in real-time
  - Generation completes within 60s
  - Results table displays 5 items
  - Validation scores visible
  - No timeout errors

**4. SSE Streaming Verification**
- During generation, open Network tab (F12)
- Find `/v1/generate-items-stream` request
- Verify:
  - Type: "eventsource"
  - Status: 200
  - Events stream incrementally (not all at once)

**5. Export Functionality**
- After generation completes
- Click "Download" dropdown
- Select "CSV"
- Verify:
  - File downloads
  - Opens in Excel/spreadsheet app
  - Contains items with validation scores

**6. Cold Start Documentation**
- Verify COLD-START-BASELINE.md exists
- Contains actual measurements (not "TBD")
- Documents DEP-08 deferral rationale

**Success criteria:**
- All 5 core verifications pass
- No CORS errors
- No timeout errors
- Export works for all formats
- Cold start baseline documented
  </how-to-verify>
  <resume-signal>
Type "approved" if all verifications pass, or describe specific issues encountered (e.g., "CORS error on frontend", "timeout after 65s", "export CSV empty")
  </resume-signal>
</task>

</tasks>

<verification>
Manual verification steps:

1. Verify deployment guide exists and is comprehensive:
   ```bash
   wc -l .planning/phases/05-vercel-deployment/DEPLOYMENT-GUIDE.md
   # Should be >100 lines
   ```

2. Verify cold start documentation exists:
   ```bash
   test -f .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md
   grep -q "DEP-08" .planning/phases/05-vercel-deployment/COLD-START-BASELINE.md
   ```

3. Review deployment guide contents:
   - Prerequisites section
   - Step-by-step backend deployment
   - Step-by-step frontend deployment
   - End-to-end verification checklist
   - Troubleshooting section
   - Known limitations documented

4. After human deployment checkpoint:
   - Production URLs documented
   - All verification checks passed
   - Any issues noted and resolved

5. After cold start measurement:
   - COLD-START-BASELINE.md updated with actual times
   - Measurements within expected range (3-8s cold, <500ms warm)

6. Final verification:
   - Backend /healthz accessible
   - Frontend loads and works
   - Item generation completes successfully
   - SSE streaming functional
   - Export downloads work
</verification>

<success_criteria>
- [ ] DEPLOYMENT-GUIDE.md created with comprehensive deployment instructions
- [ ] COLD-START-BASELINE.md created documenting DEP-08 deferral and measurement methodology
- [ ] Backend deployed to Vercel with working /healthz endpoint
- [ ] Frontend deployed to Vercel with accessible UI
- [ ] Environment variables configured (CLAUDE_API_KEY, NEXT_PUBLIC_API_URL)
- [ ] End-to-end item generation workflow tested and working
- [ ] SSE streaming displays real-time progress updates
- [ ] Export functionality works for all formats (CSV, JSON, Markdown)
- [ ] No CORS errors in production
- [ ] No timeout errors (generation completes within 300s)
- [ ] Cold start baseline measurements recorded in COLD-START-BASELINE.md
- [ ] Production URLs documented
- [ ] DEP-05, DEP-06, DEP-07 requirements satisfied
- [ ] DEP-08 deferral documented with baseline measurements
</success_criteria>

<output>
After completion, create `.planning/phases/05-vercel-deployment/05-03-SUMMARY.md`

Include in summary:
- Production backend URL
- Production frontend URL
- Deployment date and time
- Verification results (all checks passed or issues encountered)
- Cold start baseline measurements (average cold/warm times)
- Any deployment-specific notes or observations
</output>
