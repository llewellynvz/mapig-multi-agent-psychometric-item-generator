---
phase: 08-synthetic-correlation-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/schemas.py
  - src/lib/types.ts
  - backend/agents/correlation_estimator.py
  - backend/analytics/omega_calculator.py
  - backend/analytics/__init__.py
  - backend/graph.py
  - tests/test_schemas.py
  - tests/test_correlation_estimation.py
  - tests/test_omega_calculator.py
autonomous: true
requirements: [CORR-01, CORR-02, CORR-03, CORR-05, CORR-06]

must_haves:
  truths:
    - "correlation_node produces a populated CorrelationMatrix in FinalOutput after item finalization"
    - "McDonald's omega is computed from the synthetic correlation matrix with threshold flag (>= 0.70 pass)"
    - "Each CorrelationCell contains ci_low and ci_high confidence interval bounds"
    - "CorrelationMatrix.disclaimer field defaults to 'LLM-estimated, not empirically validated'"
    - "Internal consistency flag computed from mean inter-item correlation (0.15-0.50 optimal range)"
  artifacts:
    - path: "backend/agents/correlation_estimator.py"
      provides: "GPT-5.2 pairwise correlation estimation in batches"
      exports: ["estimate_pairwise_correlations"]
    - path: "backend/analytics/omega_calculator.py"
      provides: "McDonald's omega calculation from correlation matrix via reliabiliPy"
      exports: ["calculate_omega"]
    - path: "backend/graph.py"
      provides: "Real correlation_node implementation replacing placeholder"
      contains: "correlation_node"
    - path: "backend/schemas.py"
      provides: "CorrelationMatrix with mcdonalds_omega field (renamed from cronbachs_alpha)"
      contains: "mcdonalds_omega"
  key_links:
    - from: "backend/graph.py (correlation_node)"
      to: "backend/agents/correlation_estimator.py"
      via: "import and call estimate_pairwise_correlations"
      pattern: "estimate_pairwise_correlations"
    - from: "backend/graph.py (correlation_node)"
      to: "backend/analytics/omega_calculator.py"
      via: "import and call calculate_omega"
      pattern: "calculate_omega"
    - from: "backend/graph.py (correlation_node)"
      to: "state['final_output']"
      via: "populates FinalOutput.correlation_matrix with computed CorrelationMatrix"
      pattern: "correlation_matrix"
---

<objective>
Build the backend correlation analysis engine: schema migration from cronbachs_alpha to mcdonalds_omega, GPT-5.2 pairwise correlation estimation with confidence intervals, McDonald's omega calculation via reliabiliPy, and internal consistency flagging. Replace the correlation_node placeholder in graph.py with real implementation.

Purpose: This is the computational backbone of Phase 8. Without this, there is no correlation data for the frontend heatmap or calibration validation to consume.

Output: Working correlation_node that produces populated CorrelationMatrix in FinalOutput after item finalization, with all CORR requirements except CORR-04 (calibration, Plan 03).
</objective>

<execution_context>
@C:/Users/Llewellyn/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Llewellyn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-synthetic-correlation-analysis/08-CONTEXT.md
@.planning/phases/08-synthetic-correlation-analysis/08-RESEARCH.md
@.planning/phases/08-synthetic-correlation-analysis/08-VALIDATION.md

@.planning/phases/07-foundation-infrastructure/07-01-SUMMARY.md
@.planning/phases/07-foundation-infrastructure/07-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/schemas.py (Phase 7 — to be modified):
```python
class CorrelationCell(BaseModel):
    model_config = ConfigDict(extra="forbid")
    item_i_index: int = Field(..., ge=0)
    item_j_index: int = Field(..., ge=0)
    correlation: float = Field(..., ge=-1.0, le=1.0)
    ci_low: float = Field(..., ge=-1.0, le=1.0)
    ci_high: float = Field(..., ge=-1.0, le=1.0)

class CorrelationMatrix(BaseModel):
    model_config = ConfigDict(extra="forbid")
    cells: List[CorrelationCell] = Field(..., min_length=1)
    cronbachs_alpha: float = Field(..., ge=0.0, le=1.0)  # RENAME to mcdonalds_omega
    mean_inter_item_correlation: float = Field(...)
    internal_consistency_flag: str = Field(...)
    disclaimer: str = Field(default="LLM-estimated, not empirically validated")
```

From src/lib/types.ts (Phase 7 — to be modified):
```typescript
export interface CorrelationMatrix {
  cells: CorrelationCell[];
  cronbachs_alpha: number;  // RENAME to mcdonalds_omega
  mean_inter_item_correlation: number;
  internal_consistency_flag: string;
  disclaimer: string;
}
```

From backend/graph.py (Phase 7 — placeholder to replace):
```python
def correlation_node(state: GraphState) -> GraphState:
    """Placeholder for correlation analysis (Phase 8 implementation)."""
    with step("correlation_node", state):
        logger.info("Correlation analysis placeholder (Phase 8 implementation pending)")
        return {}
```

From backend/agents/llm_factory.py:
```python
def get_gpt52_analytics_model() -> ChatOpenAI:
    # Returns ChatOpenAI with model="gpt-5.2", reasoning={"effort": "high", "summary": "auto"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration and correlation estimation engine</name>
  <files>
    backend/schemas.py
    src/lib/types.ts
    backend/agents/correlation_estimator.py
    backend/analytics/__init__.py
    backend/analytics/omega_calculator.py
    tests/test_schemas.py
    tests/test_correlation_estimation.py
    tests/test_omega_calculator.py
  </files>
  <behavior>
    - Test: CorrelationMatrix.mcdonalds_omega field exists and accepts float 0.0-1.0 (cronbachs_alpha field is gone)
    - Test: CorrelationMatrix with mcdonalds_omega >= 0.70 flags as pass, < 0.70 flags as warning
    - Test: CorrelationCell stores ci_low and ci_high (CORR-03 — already exists from Phase 7, verify not broken by rename)
    - Test: CorrelationMatrix.disclaimer defaults to "LLM-estimated, not empirically validated" (CORR-05)
    - Test: calculate_omega returns omega_total, mean_inter_item_correlation, and internal_consistency_flag from a known correlation matrix
    - Test: internal_consistency_flag = "optimal_range" when mean r in 0.15-0.50, "too_low" when < 0.15, "too_high" when > 0.50
    - Test: estimate_pairwise_correlations returns list of CorrelationCell with correct indices (mock LLM)
    - Test: Batching works correctly — 10 items (45 pairs) with batch_size=20 produces 3 batches
  </behavior>
  <action>
    1. **Schema migration (breaking change per user decision):**
       - In backend/schemas.py: Rename `cronbachs_alpha` to `mcdonalds_omega` in CorrelationMatrix class. Keep same constraints (ge=0.0, le=1.0). Update description to "McDonald's omega total for internal consistency reliability".
       - In src/lib/types.ts: Rename `cronbachs_alpha` to `mcdonalds_omega` in CorrelationMatrix interface.
       - Update existing tests in tests/test_schemas.py that reference cronbachs_alpha to use mcdonalds_omega.

    2. **Install dependencies:**
       - Run `pip install reliabilipy numpy pandas` for omega calculation.
       - These are backend-only; no frontend deps needed for this plan.

    3. **Create backend/analytics/__init__.py** (empty, makes analytics a Python package).

    4. **Create backend/analytics/omega_calculator.py:**
       - Function `calculate_omega(cells: list[CorrelationCell], num_items: int) -> dict` that:
         - Builds symmetric NxN correlation matrix from flat cells list (diagonal = 1.0)
         - Converts to pandas DataFrame
         - Uses `reliability_analysis(correlations_matrix=df)` then `.fit()` to get omega_total
         - Computes mean inter-item correlation from off-diagonal values
         - Computes internal_consistency_flag: "too_low" if mean_r < 0.15, "too_high" if mean_r > 0.50, "optimal_range" otherwise (CORR-06)
         - Returns dict with omega_total, mean_inter_item_correlation, internal_consistency_flag
       - Wrap reliabiliPy call in try/except; if matrix is non-positive-definite, log warning and return omega_total=None with flag "calculation_failed"
       - Follow research pattern from 08-RESEARCH.md Pattern 2

    5. **Create backend/agents/correlation_estimator.py:**
       - Function `estimate_pairwise_correlations(items: list[str], construct_name: str, batch_size: int = 20) -> list[CorrelationCell]` that:
         - Generates all unique pairs: [(i, j) for i in range(N) for j in range(i+1, N)]
         - Batches pairs into groups of batch_size
         - For each batch, calls GPT-5.2 via `get_gpt52_analytics_model()` with structured prompt requesting correlation + 95% CI for each pair
         - Prompt includes construct name for psychometric context
         - Parses JSON response into CorrelationCell objects
         - Uses asyncio.gather for parallel batch execution (3-5 concurrent max)
         - Returns full list of CorrelationCell objects
       - Follow research pattern from 08-RESEARCH.md Patterns 1 and 4
       - Handle JSON parse errors gracefully (log and skip malformed pairs, retry batch once)

    6. **Write tests:**
       - tests/test_omega_calculator.py: Test calculate_omega with a known 5x5 correlation matrix, test internal_consistency_flag thresholds, test non-positive-definite matrix handling
       - tests/test_correlation_estimation.py: Mock LLM calls, test batching logic, test CorrelationCell construction from parsed response, test error handling for malformed JSON
       - Update tests/test_schemas.py: Fix any broken references to cronbachs_alpha, verify mcdonalds_omega field works
  </action>
  <verify>
    <automated>pytest tests/test_schemas.py tests/test_omega_calculator.py tests/test_correlation_estimation.py -x -v</automated>
  </verify>
  <done>
    - CorrelationMatrix uses mcdonalds_omega field (cronbachs_alpha removed)
    - TypeScript CorrelationMatrix interface uses mcdonalds_omega
    - calculate_omega returns omega_total from reliabiliPy with internal consistency flags
    - estimate_pairwise_correlations generates batched LLM calls returning CorrelationCell objects with CI
    - All schema tests pass after rename, omega calculator tests pass, estimation tests pass with mocked LLM
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire correlation_node in graph.py</name>
  <files>
    backend/graph.py
    tests/test_graph.py
  </files>
  <action>
    Replace the correlation_node placeholder in backend/graph.py with real implementation:

    1. **Import new modules** at top of graph.py:
       - `from backend.agents.correlation_estimator import estimate_pairwise_correlations`
       - `from backend.analytics.omega_calculator import calculate_omega`

    2. **Replace correlation_node body** (keep `with step("correlation_node", state):` wrapper for SSE events):
       - Extract finalized items from `state["final_output"].final_items` — get item texts as list[str]
       - Extract construct name from `state["user_request"]["construct_name"]` (or similar field)
       - If fewer than 3 items, skip correlation analysis (return empty dict, log "Too few items for correlation")
       - Call `await estimate_pairwise_correlations(item_texts, construct_name)` to get cells
       - Call `calculate_omega(cells, num_items)` to get omega metrics
       - Construct CorrelationMatrix with: cells=cells, mcdonalds_omega=omega_result["omega_total"], mean_inter_item_correlation=omega_result["mean_inter_item_correlation"], internal_consistency_flag=omega_result["internal_consistency_flag"]
       - If omega_total is None (non-positive-definite matrix), set mcdonalds_omega=0.0 and internal_consistency_flag="calculation_failed"
       - Update FinalOutput: create new FinalOutput with correlation_matrix populated
       - Return {"final_output": updated_final_output}
       - Make correlation_node async (change `def` to `async def`) since it calls async estimate_pairwise_correlations
       - Track token usage: after LLM calls, accumulate GPT-5.2 tokens via existing _accumulate_tokens pattern

    3. **Wrap entire correlation logic in try/except:**
       - On failure, log error, return empty dict (FinalOutput.correlation_matrix stays None)
       - This satisfies graceful failure requirement (items still generated even if correlation fails)

    4. **Update tests/test_graph.py:**
       - Update test_analytics_placeholders_no_op to reflect that correlation_node is no longer no-op
       - Add test: correlation_node with mocked estimator and calculator produces CorrelationMatrix in FinalOutput
       - Add test: correlation_node with < 3 items skips gracefully
       - Add test: correlation_node failure returns empty dict (FinalOutput unchanged)
  </action>
  <verify>
    <automated>pytest tests/test_graph.py tests/test_correlation_estimation.py tests/test_omega_calculator.py -x -v</automated>
  </verify>
  <done>
    - correlation_node calls estimate_pairwise_correlations and calculate_omega
    - FinalOutput.correlation_matrix is populated after correlation_node runs
    - Graceful failure: if correlation estimation or omega calculation fails, FinalOutput.correlation_matrix remains None and item generation completes
    - Graph topology unchanged (finalize -> correlation -> comparison -> cross_construct -> END)
    - All graph tests pass
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_schemas.py -x` — all schema tests pass with mcdonalds_omega rename
2. `pytest tests/test_omega_calculator.py -x` — omega calculation with known matrix produces valid result
3. `pytest tests/test_correlation_estimation.py -x` — batched estimation with mocked LLM works
4. `pytest tests/test_graph.py -x` — correlation_node integration produces CorrelationMatrix
5. `npx tsc --noEmit --strict src/lib/types.ts` — TypeScript compiles after mcdonalds_omega rename
6. `python -c "from backend.graph import build_graph; g = build_graph(); print('OK')"` — graph builds successfully
</verification>

<success_criteria>
- CorrelationMatrix schema uses mcdonalds_omega (not cronbachs_alpha) in both Python and TypeScript
- Running the full generation pipeline produces a populated correlation_matrix in FinalOutput (with mocked LLM or live GPT-5.2)
- Omega threshold flagging works: >= 0.70 = pass, < 0.70 = warning
- Internal consistency flags work: mean r in 0.15-0.50 = optimal, < 0.15 = too_low, > 0.50 = too_high
- All existing tests still pass (no regression from schema rename)
- Graceful failure: correlation errors do not block item generation
</success_criteria>

<output>
After completion, create `.planning/phases/08-synthetic-correlation-analysis/08-01-SUMMARY.md`
</output>
