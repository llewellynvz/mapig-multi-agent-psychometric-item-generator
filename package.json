{
  "name": "mapig-dev",
  "private": true,
  "scripts": {
    "dev": "python run_dev.py",
    "dev:sh": "./run_dev.sh",
    "backend": "uvicorn app.main:app --reload",
    "frontend": "npm --prefix frontend run dev"
  }
}
